spring:
  application:
    name: rabbitmq-springboot-demo

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

    listener:
      simple:
        default-requeue-rejected: false

server:
  port: 8080